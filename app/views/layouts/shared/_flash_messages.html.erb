<% if flash.any? %>
  <% flash.each do |type, message| %>
    <% type = "success" if "notice" == type %>
    <% type = "danger"  if "alert"  == type %>

    <%= tag.div message, class: "alert alert-#{type}" %>

  <% end %>
<% end %>
